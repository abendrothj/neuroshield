apiVersion: v1
kind: Service
metadata:
  name: neurashield-ai
  labels:
    app: neurashield
    component: ai
spec:
  type: ClusterIP
  ports:
  - port: 3002
    targetPort: 3002
    protocol: TCP
    name: http
  selector:
    app: neurashield
    component: ai 