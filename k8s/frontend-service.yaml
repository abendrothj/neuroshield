apiVersion: v1
kind: Service
metadata:
  name: neurashield-frontend
  labels:
    app: neurashield
    component: frontend
spec:
  type: ClusterIP
  ports:
  - port: 80
    targetPort: 3000
    protocol: TCP
    name: http
  selector:
    app: neurashield
    component: frontend 