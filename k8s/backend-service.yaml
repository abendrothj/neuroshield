apiVersion: v1
kind: Service
metadata:
  name: neurashield-backend
  labels:
    app: neurashield
    component: backend
spec:
  type: ClusterIP
  ports:
  - port: 3001
    targetPort: 3001
    protocol: TCP
    name: http
  selector:
    app: neurashield
    component: backend 